#!/bin/sh

cat << EOF
---
config_vars:
  PATH: "/usr/local/bin:/usr/bin:/bin:/app/vendor/flex/bin"
  FLEX_HOME: "/app/vendor/lfex"
  TMPDIR: "/app/tmp"
default_process_types:
  console: "vendor/flex/bin/mxmlc"
EOF
